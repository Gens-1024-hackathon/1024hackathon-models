{
  "name": "kampare-models",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "pouchdb": "^5.1.0"
  },
  "devDependencies": {
    "babelify": "^6.4.0",
    "browserify": "^12.0.1",
    "uglifyify": "^3.0.1"
  },
  "scripts": {
    "pack": "browserify -t babelify index.js -o dist/models.js",
    "pack:release": "browserify -t babelify -g uglifyify index.js -o dist/models.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "GG",
  "license": "ISC"
}
